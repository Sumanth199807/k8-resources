apiVersion: v1
kind: Service
metadata:
  name: nginx-up

  labesl:
    project: roboshop
    environment: dev

spec:
  type: NodePort
  selector:
    project: roboshop
    tier: frontend
    component: frontend

  ports:
   - protocol: TCP
     port: 80
     targetPort: 80
     nodePort: 3007
     